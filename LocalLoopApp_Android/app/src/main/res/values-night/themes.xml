<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.LocalLoopApp_Android" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="buttonStyle">@style/CustomButton</item>
    </style>

    <style name="Theme.LocalLoopApp_Android" parent="Base.Theme.LocalLoopApp_Android" />

    <!-- Dark theme button style (same drawable or separate if needed) -->
    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_primary</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>
